pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "MagisterAPI"
include(":magisterapi")
include("convention-plugins")
